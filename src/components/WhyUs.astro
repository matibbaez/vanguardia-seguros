<section id="por-que-elegirnos" class="section why-us-section">
    <div class="container">
        
        <div class="header-center reveal">
            <span class="badge">Por qué elegirnos</span>
            <h2 class="title-lg">
                Seguros diseñados para <br> <span class="text-gradient">tu velocidad</span>
            </h2>
        </div>

        <div class="bento-grid">
            
            <div class="bento-item item-main dark-card reveal reveal-delay-1">
                <div class="content">
                    <div class="badge-tech">⚡ Rapidez</div>
                    <h3>Cotizá sin vueltas</h3>
                    <p class="text-white-muted">Olvídate de formularios del año 2000. Escribinos y tené tu precio al instante.</p>
                </div>
                
                <div class="chat-simulation">
                    <div class="chat-bubble user">Hola, quiero cotizar mi auto 👋</div>
                    <div class="chat-bubble bot">
                        <div class="typing-dots">
                            <span></span><span></span><span></span>
                        </div>
                    </div>
                    <div class="chat-bubble bot delayed">¡Dale! Es un Toyota Corolla 2023?</div>
                    
                    <div class="chat-notification">
                        <div class="icon-check">✓</div>
                        <span>Presupuesto listo en 2 min</span>
                    </div>
                </div>
            </div>

            <div class="bento-item item-square reveal reveal-delay-2" id="asesoria">
                <div class="content flex-col h-full justify-between">
                    <div>
                        <div class="icon-box-soft">⚖️</div>
                        <h3>Asesoramiento Jurídico</h3>
                        <p class="text-muted text-sm">Nuestro equipo legal te respalda sin costo adicional.</p>
                    </div>
                    
                    <div class="support-profile" style="background: #eff6ff;">
                        <div class="support-info" style="width: 100%;">
                            <strong>Servicio Gratuito</strong>
                            <span style="color: var(--primary);">Para todos nuestros clientes</span>
                        </div>
                    </div>
                </div>
            </div>

            <div class="bento-item item-square reveal reveal-delay-2" style="background: linear-gradient(135deg, #ecfdf5 0%, #ffffff 100%); border-color: #10b981;">
                <div class="content text-center flex-col flex-center h-full">
                    <span style="font-size: 3.5rem; font-weight: 800; color: #059669; line-height: 1;">-30%</span>
                    <h3 style="margin-top: 0.5rem; color: #065f46;">Ahorro Promedio</h3>
                    <p class="text-muted text-sm">Comparando entre +10 aseguradoras líderes.</p>
                </div>
            </div>

            <div class="bento-item item-wide reveal reveal-delay-3">
                <div class="wide-content">
                    <div class="text-side">
                        <h3>Gestión de Siniestros 360°</h3>
                        <p class="text-muted">Si chocás, relajá. Nosotros hacemos el trabajo sucio.</p>
                    </div>
                    
                    <div class="steps-visual">
                        <div class="step">
                            <div class="step-icon">💥</div>
                            <span>Choque</span>
                        </div>
                        <div class="step-line"></div>
                        <div class="step">
                            <div class="step-icon">📸</div>
                            <span>Foto</span>
                        </div>
                        <div class="step-line"></div>
                        <div class="step">
                            <div class="step-icon active">🛡️</div>
                            <span>Solución</span>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>
</section>

<style>
    /* --- HEADER STYLES --- */
    .header-center { 
        text-align: center;
        max-width: 800px; 
        margin: 0 auto 3rem auto; 
    }
  
    .badge {
        display: inline-block;
        background: #eff6ff; 
        color: var(--primary);
        padding: 6px 16px; 
        border-radius: 50px; 
        font-weight: 700; 
        font-size: 0.8rem;
        margin-bottom: 1rem; 
        text-transform: uppercase; 
        letter-spacing: 1px;
    }

    /* --- SECCIÓN PRINCIPAL --- */
    .why-us-section {
        background-color: white;
        border-top: 1px solid var(--border); 
        box-shadow: inset 0 10px 15px -10px rgba(0,0,0,0.05);
    }

    /* --- GRID BASE --- */
    .bento-grid {
        display: grid;
        grid-template-columns: 1fr;
        gap: 1.5rem;
    }

    /* --- TARJETAS --- */
    .bento-item {
        background: white;
        border-radius: 24px;
        padding: 2rem;
        border: 1px solid #e2e8f0;
        position: relative;
        overflow: hidden;
        transition: all 0.3s ease;
        display: flex;
        flex-direction: column;
    }

    .bento-item:hover {
        transform: translateY(-5px);
        box-shadow: 0 15px 30px -5px rgba(0,0,0,0.08);
        border-color: var(--primary-hover);
    }

    /* --- ITEM 1: DARK CARD (CHAT) --- */
    .dark-card {
        background: var(--primary);
        color: white;
        border: none;
        min-height: 450px;
    }
    
    .badge-tech {
        display: inline-block;
        background: rgba(255,255,255,0.1); padding: 4px 12px;
        border-radius: 20px; font-size: 0.8rem; font-weight: 600; margin-bottom: 1rem;
        border: 1px solid rgba(255,255,255,0.2);
    }
    .dark-card h3 { color: white; margin-bottom: 0.5rem; }
    .text-white-muted { color: rgba(255,255,255,0.7);
        font-size: 0.95rem; margin-bottom: 1rem; }

    /* CHAT SIMULATION */
    .chat-simulation {
        background: rgba(255,255,255,0.05);
        border-radius: 16px 16px 0 0;
        padding: 1.5rem;
        padding-bottom: 90px; 
        display: flex; flex-direction: column; gap: 12px;
        flex-grow: 1; 
        position: relative;
        mask-image: linear-gradient(to bottom, black 85%, transparent 100%);
    }

    .chat-bubble {
        padding: 12px 16px;
        border-radius: 12px; font-size: 0.9rem; max-width: 85%;
        opacity: 0; animation: popIn 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards;
    }
    .chat-bubble.user { background: rgba(255,255,255,0.2); align-self: flex-end; color: white; border-bottom-right-radius: 2px; animation-delay: 0.2s; }
    .chat-bubble.bot { background: white; color: var(--primary); align-self: flex-start; border-bottom-left-radius: 2px; font-weight: 600; animation-delay: 1s; }
    .chat-bubble.delayed { animation-delay: 2.5s; }
    
    .typing-dots span { display: inline-block;
        width: 4px; height: 4px; background: var(--primary); border-radius: 50%; animation: typing 1.4s infinite; margin: 0 1px;
    }

   .chat-notification {
        position: absolute; bottom: 25px; right: 20px;
        background: var(--accent); 
        color: white; 
        padding: 10px 18px; border-radius: 50px;
        font-size: 0.85rem; font-weight: 700; display: flex; align-items: center; gap: 8px;
        box-shadow: 0 10px 20px rgba(255, 102, 0, 0.25);
        opacity: 0; animation: slideUp 0.5s forwards 3.5s; z-index: 10;
    }

    /* --- ITEM 2: SOPORTE --- */
    .h-full { height: 100%; }
    .justify-between { justify-content: space-between; }
    .flex-col { display: flex; flex-direction: column; }
    .text-sm { font-size: 0.9rem; }
    .icon-box-soft { font-size: 2rem; margin-bottom: 0.5rem; }

    .support-profile {
        display: flex; align-items: center; gap: 15px;
        background: #f1f5f9; padding: 12px; border-radius: 16px; margin-top: 1rem;
    }

    .avatar-group { position: relative; width: 50px; height: 50px; flex-shrink: 0; }
    .avatar { width: 100%; height: 100%; border-radius: 50%; object-fit: cover; border: 2px solid white; }
    .status-dot { width: 12px; height: 12px; background: #22c55e; border-radius: 50%; border: 2px solid white; position: absolute; bottom: 2px; right: 0; }
    .support-info strong { display: block; font-size: 0.95rem; color: var(--text-main); line-height: 1.2; }
    .support-info span { font-size: 0.8rem; color: #16a34a; font-weight: 700; }

    /* --- ITEM 3 & 4 --- */
    .flex-center { display: flex; align-items: center; justify-content: center; }
    
    .wide-content { display: flex; justify-content: space-between; align-items: center; gap: 2rem; }
    .steps-visual { display: flex; align-items: center; gap: 5px; }
    .step { display: flex; flex-direction: column; align-items: center; gap: 6px; }
    .step-icon { width: 40px; height: 40px; background: white; border: 1px solid #e2e8f0; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 1.2rem; z-index: 1; }
    .step-icon.active { background: var(--primary); color: white; border-color: var(--primary); }
    .step span { font-size: 0.75rem; font-weight: 600; color: var(--text-muted); }
    .step-line { flex-grow: 1; height: 2px; background: #e2e8f0; width: 25px; margin-bottom: 20px; }

    /* --- ANIMACIONES --- */
    @keyframes popIn { to { opacity: 1; } }
    @keyframes slideUp { to { opacity: 1; transform: translateY(0); } }
    @keyframes typing { 0%, 100% { transform: scale(0); } 50% { transform: scale(1); } }

    /* --- RESPONSIVE DESKTOP --- */
    @media (min-width: 768px) {
        .bento-grid {
            grid-template-columns: repeat(3, 1fr);
            grid-template-rows: 240px 200px auto;
        }
        .item-main { grid-column: 1 / 3; grid-row: 1 / 3; min-height: auto; }
        .item-square:nth-child(2) { grid-column: 3 / 4; grid-row: 1 / 2; }
        .item-square:nth-child(3) { grid-column: 3 / 4; grid-row: 2 / 3; }
        .item-wide { grid-column: 1 / -1; grid-row: 3 / 4; }
        
        .support-profile { margin-top: 0; }
        .chat-simulation { padding-bottom: 20px; }
    }

    /* --- RESPONSIVE MOBILE --- */
    @media (max-width: 768px) {
        .wide-content { flex-direction: column; text-align: center; gap: 1.5rem; }
        .steps-visual { width: 100%; justify-content: space-around; }
        .step-line { width: auto; flex-grow: 1; }
    }
</style>