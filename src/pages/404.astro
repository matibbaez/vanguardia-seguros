---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Página no encontrada | Vanguardia">
  <section class="error-container">
    <div class="content reveal">
      <span class="big-404">404</span>
      <h1>Ups... chocaste con un <span class="text-gradient">link roto</span></h1>
      <p>No te preocupes, no hace falta llamar a la grúa. Volvamos al camino seguro.</p>
      
      <a href="/" class="btn-home">
        Volver al Inicio
      </a>
    </div>
  </section>
</Layout>

<style>
  .error-container {
    height: 80vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    background: radial-gradient(circle at center, #f8fafc 0%, #e2e8f0 100%);
  }

  .big-404 {
    font-size: 8rem;
    font-weight: 800;
    color: var(--primary);
    opacity: 0.1;
    line-height: 1;
    display: block;
  }

  h1 { font-size: 2.5rem; margin-bottom: 1rem; font-weight: 800; }
  p { color: var(--text-muted); margin-bottom: 2rem; font-size: 1.1rem; }

  .btn-home {
    display: inline-block;
    background: var(--primary);
    color: white;
    padding: 12px 30px;
    border-radius: 50px;
    font-weight: 700;
    transition: transform 0.2s;
  }
  .btn-home:hover { transform: translateY(-3px); background: var(--primary-hover); }
</style>