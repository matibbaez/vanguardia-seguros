---
import Layout from '../layouts/Layout.astro';

const companies = [
  { name: 'Allianz', phone: '0800-888-24324', color: '#003781' },
  { name: 'Zurich', phone: '0800-333-987424', color: '#2167AE' },
  { name: 'Sancor', phone: '0800-444-2850', color: '#00AEEF' },
  { name: 'San Cristóbal', phone: '0810-222-8887', color: '#0047BB' },
  { name: 'Mercantil Andina', phone: '0800-888-4488', color: '#6A4C9C' },
  { name: 'Provincia', phone: '0810-222-2444', color: '#FF6600' },
  { name: 'La Caja', phone: '0800-555-0000', color: '#E31E24' },
  { name: 'Federación Patronal', phone: '0800-222-0022', color: '#00548F' },
  { name: 'Rivadavia', phone: '0800-444-0234', color: '#003F72' },
];
---

<Layout title="Siniestros | Vanguardia Seguros" isSiniestros={true}>
  
  <section class="section siniestros-hero">
    <div class="container">
      <div class="hero-center">
        
        <div class="status-badge animate-up">
            <span class="pulse"></span> Centro de Ayuda
        </div>
        
        <h1 class="title-lg animate-up" style="animation-delay: 0.1s;">
            ¿Tuviste un incidente? <br> 
            <span class="text-gradient">Mantené la calma.</span>
        </h1>
        
        <p class="text-lead animate-up" style="animation-delay: 0.2s;">
            Lo primero es tu seguridad. Si hay heridos, llamá al <strong>107</strong> o <strong>911</strong>.
            Si son solo daños materiales, seguí estos pasos.
        </p>
        
        <div class="emergency-actions animate-up" style="animation-delay: 0.3s;">
          <a href="https://wa.me/5491162959939?text=URGENTE%20CHOQUE:%20Necesito%20asistencia%20inmediata" target="_blank" class="btn btn-whatsapp-lg">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="currentColor"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.008-.57-.008-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z"/></svg>
            Reportar Ahora
          </a>
          <a href="#telefonos" class="btn btn-outline-danger">
            Ver teléfonos 0800
          </a>
        </div>

      </div>
    </div>
  </section>

  <section id="pasos" class="section steps-section">
    <div class="container">
      <div class="section-header text-center animate-up">
          <span class="badge-alert">Pasos a seguir</span>
          <h2 class="title-md">Qué hacer en el momento</h2>
          <p class="text-muted mb-5">Prestá <strong>atención</strong> a estos pasos.</p>
      </div>
      
      <div class="steps-wrapper">
        
        <div class="step-item animate-up" style="animation-delay: 0.1s;">
            <div class="step-header">
                <span class="step-number">1</span>
                <div class="step-connector"></div>
            </div>
            <div class="step-card">
                <h3>Datos del Tercero</h3>
                <p>Pedile y anotá: Nombre completo, DNI, Teléfono, Marca/Modelo y Patente del auto, y su Compañía de Seguros.</p>
                <div class="tip-box">
                    <span class="icon">💡</span> Tip: Sacale foto a su tarjeta verde y registro.
                </div>
            </div>
        </div>

        <div class="step-item animate-up" style="animation-delay: 0.2s;">
            <div class="step-header">
                <span class="step-number">2</span>
                <div class="step-connector"></div>
            </div>
            <div class="step-card">
                <h3>Fotos del Lugar</h3>
                <p>Sacá fotos panorámicas donde se vea la posición de los autos <strong>antes de moverlos</strong> (si es posible) y detalles de los daños.</p>
            </div>
        </div>

        <div class="step-item animate-up" style="animation-delay: 0.3s;">
            <div class="step-header">
                <span class="step-number">3</span>
                </div>
            <div class="step-card">
                <h3>No asumas culpas</h3>
                <p>No firmes nada ni reconozcas responsabilidad en el momento de nervios. Solo intercambiá datos obligatorios.</p>
            </div>
        </div>

      </div>
    </div>
  </section>

  <section id="telefonos" class="section administrative-section">
    <div class="container">
      <div class="section-header text-center animate-up">
          <span class="badge-alert">Denuncia Administrativa</span>
          <h2 class="title-md">Llamá a tu compañía</h2>
          <p class="text-muted mb-5">Tenés <strong>72hs hábiles</strong> para realizar la denuncia.</p>
      </div>
      
      <div class="companies-grid">
        {companies.map((company, index) => (
          <div class="company-card animate-up" style={`animation-delay: ${0.1 * index}s; --brand-color: ${company.color}`}>
            <div class="card-top">
                <span class="company-name">{company.name}</span>
                <div class="indicator" style={`background: ${company.color}`}></div>
            </div>
            <a href={`tel:${company.phone}`} class="btn-call">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
                {company.phone}
            </a>
          </div>
        ))}
      </div>
  
      <div class="text-center mt-5 animate-up">
        <p class="text-sm text-muted">¿Tu compañía no está en la lista? <a href="https://wa.me/5491162959939" class="link-underline">Escribinos y te ayudamos</a>.</p>
      </div>
    </div>
  </section>

</Layout>

<style>
  /* --- ESTILOS GENERALES --- */
  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  .animate-up { opacity: 0; animation: fadeInUp 0.8s cubic-bezier(0.2, 0.8, 0.2, 1) forwards; }

  /* HERO - CORRECCIÓN DE FONDO */
  .siniestros-hero {
    text-align: center;
    
    /* 1. ESTO HACE LA MAGIA: Tira la sección hacia arriba para anular el padding del Layout */
    margin-top: calc(var(--header-height) * -1); 
    
    /* 2. COMPENSACIÓN: Empujamos el contenido hacia abajo para que no quede tapado por el menú */
    /* Header (70px) + TopBar (40px) + Aire extra (80px) */
    padding-top: calc(var(--header-height) + 120px); 
    
    padding-bottom: 80px;
    background: radial-gradient(circle at 50% 0%, #fff1f2 0%, #fff 70%); /* Degradado Rosita Suave */
    position: relative;
    z-index: 1;
  }

  /* TEXTO */
  .siniestros-hero .text-lead {
      text-align: center;
      max-width: 700px;
      margin: 1.5rem auto 2.5rem auto; /* Más aire en los márgenes */
      color: var(--text-muted);
      font-size: 1.15rem;
      line-height: 1.6;
  }

  /* BADGE ANIMADO */
  .status-badge {
    display: inline-flex; align-items: center; gap: 8px;
    background: white; border: 1px solid #fee2e2; color: #ef4444;
    padding: 8px 20px; border-radius: 50px; font-weight: 700; font-size: 0.9rem;
    margin-bottom: 2rem;
    box-shadow: 0 4px 15px rgba(254, 226, 226, 0.6);
  }
  
  .pulse {
    width: 10px; height: 10px; background: #ef4444; border-radius: 50%;
    box-shadow: 0 0 0 0 rgba(239, 68, 68, 0.7);
    animation: pulse-red 2s infinite;
  }
  
  @keyframes pulse-red {
    0% { transform: scale(0.95); box-shadow: 0 0 0 0 rgba(239, 68, 68, 0.7); }
    70% { transform: scale(1); box-shadow: 0 0 0 8px rgba(239, 68, 68, 0); }
    100% { transform: scale(0.95); box-shadow: 0 0 0 0 rgba(239, 68, 68, 0); }
  }

  /* BOTONES DE ACCIÓN */
  .emergency-actions {
    display: flex;
    justify-content: center; gap: 1.5rem; margin-top: 1rem; flex-wrap: wrap;
  }

  .btn-whatsapp-lg {
    background-color: #25D366; color: white;
    border-radius: 50px; font-weight: 700;
    padding: 1rem 2.5rem; font-size: 1.1rem; gap: 12px;
    box-shadow: 0 10px 25px rgba(37, 211, 102, 0.3);
    display: inline-flex; align-items: center; transition: all 0.3s ease;
    border: none;
  }
  .btn-whatsapp-lg:hover { background-color: #20bd5a; transform: translateY(-3px); box-shadow: 0 15px 35px rgba(37, 211, 102, 0.4); }

  .btn-outline-danger {
    background: white; border: 2px solid #e2e8f0; color: var(--text-main);
    padding: 1rem 2.5rem; border-radius: 50px;
    font-weight: 700; transition: all 0.3s ease;
    display: inline-flex; align-items: center; text-decoration: none;
  }
  .btn-outline-danger:hover { border-color: #ef4444; color: #ef4444; background: #fff5f5; }

  /* STEPS SECTION */
  .steps-section { background: white; padding-top: 5rem; padding-bottom: 5rem;}

  /* GRID DE PASOS */
  .steps-wrapper {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 3rem;
    margin-top: 3rem;
    position: relative;
  }

  .step-item {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  /* HEADER DEL PASO (Número + Línea) */
  .step-header {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 100%;
    margin-bottom: 2rem;
  }

  .step-number {
    width: 60px; height: 60px;
    background: var(--primary);
    color: white;
    border-radius: 50%;
    font-size: 1.5rem;
    font-weight: 800;
    display: flex; align-items: center; justify-content: center;
    box-shadow: 0 0 0 8px white, 0 10px 25px rgba(var(--primary-rgb), 0.2);
    position: relative;
    z-index: 2;
  }

  .step-connector {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 100%;
    height: 3px;
    background: #e2e8f0;
    z-index: 1;
  }

  .step-card {
    background: white;
    padding: 2.5rem;
    border-radius: 24px;
    border: 1px solid #f1f5f9;
    box-shadow: 0 10px 40px -10px rgba(0,0,0,0.05);
    transition: transform 0.3s ease;
    height: 100%;
    width: 100%;
  }
  .step-card:hover { transform: translateY(-5px); border-color: var(--primary); }

  .step-card h3 { font-size: 1.4rem; font-weight: 800; margin-bottom: 1rem; color: var(--text-main); }
  .step-card p { font-size: 1rem; color: var(--text-muted); line-height: 1.6; }

  .tip-box {
    margin-top: 1.5rem;
    background: #fff7ed;
    color: #c2410c;
    padding: 12px 18px;
    border-radius: 12px;
    font-size: 0.9rem;
    font-weight: 600;
    text-align: left;
    border-left: 4px solid #f97316;
  }

  /* TELEFONOS GRID */
  .administrative-section { background: #f8fafc; border-top: 1px solid var(--border); padding-top: 6rem; padding-bottom: 6rem; }
  
  .section-header { margin-bottom: 4rem; }
  .badge-alert {
    display: inline-block; background: #dbeafe;
    color: #1e40af; padding: 6px 16px; border-radius: 20px;
    font-size: 0.85rem; font-weight: 800; text-transform: uppercase; margin-bottom: 1.5rem; letter-spacing: 1px;
  }

  .companies-grid {
    display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 2rem;
  }

  .company-card {
    background: white; padding: 2rem; border-radius: 20px;
    border: 1px solid #e2e8f0; transition: all 0.2s;
    display: flex; flex-direction: column; justify-content: space-between; gap: 1.5rem;
    height: 100%; box-shadow: 0 4px 6px rgba(0,0,0,0.02);
  }
  .company-card:hover { transform: translateY(-5px); box-shadow: 0 20px 40px rgba(0,0,0,0.08); border-color: var(--brand-color); }

  .card-top { display: flex; justify-content: space-between; align-items: center; }
  .company-name { font-weight: 800; font-size: 1.2rem; color: var(--text-main); }
  .indicator { width: 14px; height: 14px; border-radius: 50%; box-shadow: 0 0 0 4px rgba(0,0,0,0.05); }

  .btn-call {
    display: flex; align-items: center; justify-content: center; gap: 10px;
    background: #f8fafc; color: var(--text-main);
    font-weight: 700;
    padding: 14px; border-radius: 14px; transition: all 0.2s; text-decoration: none;
    border: 1px solid transparent;
  }
  .btn-call:hover { background: var(--brand-color); color: white; border-color: var(--brand-color); box-shadow: 0 5px 15px rgba(0,0,0,0.1); }
  
  .link-underline { color: var(--primary); text-decoration: underline; font-weight: 700;}

  /* UTILITIES */
  .mb-5 { margin-bottom: 5rem; }
  .mt-5 { margin-top: 4rem; }

  /* ADAPTACIÓN MOBILE */
  @media (max-width: 900px) {
    .siniestros-hero { 
        padding-top: 140px; /* Ajuste para mobile */
        padding-bottom: 40px;
    } 
    
    .mb-5 { margin-bottom: 3rem; }

    .steps-wrapper {
        display: flex;
        flex-direction: column;
        gap: 0;
        padding-left: 20px;
    }

    /* Línea Vertical Continua Mobile */
    .steps-wrapper::before {
        content: '';
        position: absolute;
        top: 30px; bottom: 30px; left: 49px; /* Ajustado al centro del círculo */
        width: 2px;
        background: #e2e8f0;
        z-index: 0;
    }

    .step-item {
        flex-direction: row;
        align-items: flex-start;
        text-align: left;
        margin-bottom: 3rem;
    }
    .step-item:last-child { margin-bottom: 0; }

    .step-header {
        width: auto;
        margin-bottom: 0;
        margin-right: 2rem;
        flex-shrink: 0;
    }

    .step-connector { display: none; }

    .step-number { width: 60px; height: 60px; font-size: 1.5rem; }

    .step-card { padding: 1.5rem; }
    
    .emergency-actions { flex-direction: column; gap: 15px; width: 100%; }
    .btn-whatsapp-lg, .btn-outline-danger { width: 100%; justify-content: center; }
    
    .companies-grid { grid-template-columns: 1fr; }
  }
</style>